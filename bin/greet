#!/usr/bin/env ruby    /shebang line, tells terminal which interpreter to use/
require_relative "../lib/greeting.rb"
# code your CLI here!

# Make sure bin greet has code that can output message
puts "Hi! I'm HAL, what's your name?"
# Use gets.strip method to capture user's input and set equal to variable name
name = gets.strip
# greeting method with an argument of name
greeting(name)
# application returns new greeting
puts "Hello #{name}. It's nice to meet you."
