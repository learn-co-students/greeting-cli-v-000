#!/usr/bin/env ruby
require_relative "../lib/greeting.rb"

# code your CLI here!
puts "Hi! I'm HAL, what's your name?"
name = gets.strip # example the user types "amanda" so it's like saying name = "enoch"
greeting(name) # method calls pass in value so it's like calling greeting("enoch")
